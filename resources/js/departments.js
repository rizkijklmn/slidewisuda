export default () => ({
    // async init() {
    //     this.departmens = await (await fetch(`/faculties/${faculty}`, {
    //         headers: {
    //             'Accept': 'application/json'
    //         }
    //     })).json();
    // },
    departmens: [],

});
